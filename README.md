# HowGood Application Script

Signs and submits an application payload to HowGood's `/apply` endpoint.

## Setup

```bash
uv venv
# activate the virtual environment
uv pip install -r requirements.txt

cp .env.sample .env
```

## Run

```bash
python main.py
```
